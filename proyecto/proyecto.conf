server {
    listen 80;
    server_name localhost;

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        root "/home/aylen/Documentos/IS2/proyectoIS/proyecto";
    }

    location / {
        include proxy_params;
        proxy_pass "http://unix:/home/aylen/Documentos/IS2/proyectoIS/proyecto/proyecto.sock";
    }
}

server {
    listen 8001;
    server_name localhost;

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        root "/home/aylen/Documentos/IS2/proyectoIS/tauser";
    }

    location / {
        include proxy_params;
        proxy_pass "http://unix:/home/aylen/Documentos/IS2/proyectoIS/tauser/tauser.sock";
    }
}